syntax on
set expandtab
set tabstop=2
set shiftwidth=2

set number  "show line numbers
set relativenumber "show relative line numbers

" set default diff comparing options to:
" ignore whitespace differences
set diffopt+=iwhite
" ignore case differences
set diffopt+=icase
